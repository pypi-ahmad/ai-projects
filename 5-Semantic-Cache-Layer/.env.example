# 5-Semantic-Cache-Layer
# Empty placeholders only. Never commit .env or real credentials.
# Configure only what you use; remove unused assignments before loading .env.
# Empty is not always equivalent to unset and can override working defaults/keys.
# src/providers/generate.py reads these for optional generation on a cache miss.
# src/ui/app.py loads .env. Ollama host, embedding model and cache paths are
# code/argument settings, not environment variables in the current implementation.

AGNESAI_API_KEY=
OPENAI_API_KEY=
OPENAI_BASE_URL=
GOOGLE_API_KEY=
