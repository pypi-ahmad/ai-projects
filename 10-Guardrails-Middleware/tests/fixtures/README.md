# Fixtures

Empty for now; Phase 1's tests are self-contained (inline strings per case). Shared
fixtures (sample PII/injection corpora reused across test files) land here once that
duplication actually shows up.
