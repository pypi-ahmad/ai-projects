"""Package root. Stages live in src/data (dataset), src/train (LoRA), src/eval (scoring), and
src/ui (Streamlit control panel). See src/data/schema.py first — every other module passes data
shaped by it."""
