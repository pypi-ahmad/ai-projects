# Fixtures

- `invoice.txt` — sample input text for the CLI example (`docs/RUNBOOK.md`), matches the `invoice_draft` schema.

More input-text / schema / expected-output triples go here once the eval
harness (`src/eval/`, see `docs/EVAL.md`) is built.
