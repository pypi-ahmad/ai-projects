# 15-Self-Correcting-RAG-Agent
# Empty placeholders only. Never commit .env or real credentials.
# Configure only what you use; remove unused assignments before loading .env.
# Empty is not always equivalent to unset and can override working defaults/keys.
# Settings in src/self_correcting_rag/config.py load a local .env.
# SEARCH_API_KEY gates web fallback; no concrete search provider is wired yet.

OLLAMA_HOST=
AGNESAI_API_KEY=
OPENAI_API_KEY=
OPENAI_BASE_URL=
GOOGLE_API_KEY=
SEARCH_API_KEY=
SEARCH_BASE_URL=
