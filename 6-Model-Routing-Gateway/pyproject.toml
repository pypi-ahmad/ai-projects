[project]
name = "model-routing-gateway"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "tiktoken>=0.7",
    "PyYAML>=6.0",
    "streamlit>=1.35",
    "pydantic>=2.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
