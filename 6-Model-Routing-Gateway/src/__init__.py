# Model Routing Gateway — root package.
# Subpackages: gateway (request/response types + complexity scoring),
# route (tier selection + provider fallback loop), providers (per-vendor HTTP
# adapters), cost (pricing + JSONL usage log), ui (Streamlit front-end).
# Start reading at gateway/models.py, then route/router.py.
