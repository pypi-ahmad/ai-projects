# 12-Prompt-Versioning-and-AB-System
# Empty placeholders only. Never commit .env or real credentials.
# Configure only what you use; remove unused assignments before loading .env.
# Empty is not always equivalent to unset and can override working defaults/keys.
# src/promptreg API, UI, CLI and scripts/seed_demo.py read these settings.
# Set process environment variables; .env is not automatically loaded.
# Leave optional paths/port unset to retain defaults; an empty port is invalid.

PROMPTREG_DB_PATH=
PROMPTREG_PROMPTS_DIR=
PROMPTREG_OUTCOMES_JSONL=
PROMPTREG_ADMIN_TOKEN=
PROMPTREG_API_PORT=
