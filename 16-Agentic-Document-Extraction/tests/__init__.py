"""Package marker for `tests`, so `tests.fake_llm` and `tests.fixtures.*` are
importable as dotted paths from anywhere in the suite. Holds no tests of its
own.

Next: tests/fake_llm.py, the shared fake used across most of the suite.
"""
