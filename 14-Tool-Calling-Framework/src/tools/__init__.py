"""Tool-calling framework: typed tool registration, model-driven dispatch,
sandboxed execution, and the HTTP/UI surfaces around it.

Start at registry.py (how a tool gets registered and discovered) or
loop.py (how one model turn becomes a validated, executed tool call),
depending on what you're trying to understand.
"""
