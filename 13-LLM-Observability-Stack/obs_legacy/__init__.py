# Superseded by src/obs/trace + src/obs/export (a flat trace-record schema
# predating the current span-based model - see docs/ARCHITECTURE.md).
# Nothing under src/ or scripts/ imports this package. Kept only because
# tests/test_redact.py and tests/test_storage.py still exercise it.
